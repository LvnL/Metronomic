package com.example.metronomic.events

import android.app.Activity
import android.content.Context
import android.widget.EditText
import androidx.appcompat.app.AppCompatActivity
import com.example.metronomic.R
import com.example.metronomic.timing.changeBPM

fun startListeners(context: Context) {
    bpmTextBoxListener(context.applicationContext)
}

private fun bpmTextBoxListener(context: Context) {

}